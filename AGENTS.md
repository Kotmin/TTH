# Agent Conventions

1. All local build, test, and run operations use Docker only — no host Java or Gradle required.
2. All git commit messages follow Conventional Commits 1.0.0.
3. Every logical unit of behavior must have focused unit tests (JUnit 5).
4. Do not add code comments unless they serve a genuine documentation purpose.
